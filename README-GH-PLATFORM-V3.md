# Gold Hunter Platform V3

Changes in this build:

- Gold Hunter logo/menu navbar is now the top-most fixed header.
- Live Market Sessions strip is fixed directly below the navbar.
- Mobile spacing is adjusted so neither header overlaps the other.
- The Hero starts below both fixed headers.
- ISM Manufacturing and ISM Services events are removed from the calendar, including old KV-stored records.
- Existing News Engine, Forecast Admin, Gallery, XAU/DXY quote persistence and ETF module are preserved.

Deploy this build to `feature/news-engine` and test the Cloudflare Preview before merging to `main`.
